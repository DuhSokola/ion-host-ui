;(function(){
    'use strict';

    var deps = [
        'app.catalog.ctrl'
    ];

    var catalog = angular.module('app.catalog',deps);

    catalog.config(function(){

    });

    catalog.run(function(){

    });

}());
